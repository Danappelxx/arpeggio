Template.dashboard.helpers({
    classes: () => {
        // use Mongo.Collection later, once we have it setup
        return [
            {
                "name" : "World Lit Honors",
                "period" : "Period 1"
            }
        ]
    }
})
